#include "brainrotc.h"
#include <iostream>

void yapping(const std::string& text) {
    std::cout << text;
}

int skibidi(int x, int y) {
    return x + y;
}

void rizzes(const std::string& name) {
    std::cout << name << " is rizzed :)" << std::endl;
}

void gyat(const std::string& name) {
    std::cout << name << " have big gyat" << std::endl;
}